(function(){
    document.getElementById("add-song").addEventListener("submit", (event) => {
        let form = event.target.closest("form");
        if(form.checkValidity() === true){

        }else{
            
        }
    });
})();